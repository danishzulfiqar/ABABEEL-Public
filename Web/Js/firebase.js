// Author: Danish Zulfiqar
// Last updated: 25/7/23

// Firebase Config
const firebaseConfig = {
    // Add your own credidentials
};

// Initialize Firebase
firebase.initializeApp(firebaseConfig);

const auth = firebase.auth();
const database = firebase.database();
